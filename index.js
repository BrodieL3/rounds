// Expo Router runtime entry is package.json main: expo-router/entry.
// Keep root index.js neutral so Expo Go and native builds use expo-router/entry
// from package.json instead of legacy registerRootComponent scaffolding.
